language: bash

env:
  global:
    - DOCKER_IMAGE="polish-dictionary"
    - DICTIONARY="dist/pl.txt"

sudo: required

git:
  depth: false

services:
  - docker

before_script:
  - exit 1

script:
  - ./scripts/docker_run.sh
  - ./scripts/deploy.sh # TODO: Skip when travis is pushing to repo or it's pull request
